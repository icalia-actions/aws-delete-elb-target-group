{
  "name": "@icalialabs/aws-delete-elb-target-group",
  "version": "0.0.1",
  "description": "Finds or creates an AWS ELB Target Group",
  "main": "lib/main.js",
  "scripts": {
    "debug-action": "yarn run build && node -r dotenv/config index.js",
    "build": "tsc && ncc build",
    "format": "prettier --write **/*.ts",
    "format-check": "prettier --check **/*.ts",
    "test": "jest --coverage",
    "pre-checkin": "yarn run format && yarn run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/icalia-actions/aws-delete-elb-target-group.git"
  },
  "keywords": [
    "actions",
    "icalia",
    "aws",
    "ecs",
    "build",
    "push"
  ],
  "author": "Icalia Labs",
  "contributors": [
    {
      "name": "Vov",
      "url": "https://ical.ink/vov"
    }
  ],
  "license": "MIT",
  "devDependencies": {
    "@actions/core": "^1.2.7",
    "@types/jest": "^26.0.22",
    "@types/node": "^14.14.41",
    "@vercel/ncc": "^0.28.3",
    "aws-sdk": "^2.888.0",
    "dotenv": "^8.2.0",
    "jest": "^26.6.3",
    "jest-runtime": "^26.6.3",
    "prettier": "^2.2.1",
    "ts-jest": "^26.5.5",
    "typescript": "^4.2.4",
    "typescript-formatter": "^7.2.2",
    "yaml": "^1.10.2"
  }
}
